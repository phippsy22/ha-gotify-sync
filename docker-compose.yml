version: "3"
services:
  gotify:
    image: gotify/server:2.8.0
    container_name: gotify-test
    ports:
      - "8888:80"
    environment:
      - GOTIFY_DEFAULTUSER_NAME=admin
      - GOTIFY_DEFAULTUSER_PASS=admin
    volumes:
      - gotify_data:/app/data

volumes:
  gotify_data:
